// Notación
// ; -> Al final de una línea
const b = 4;
b + 4;

// . -> Se utiliza en los objetos para acceder a los atributos
// movil.anchura
// [] -> Listas, arreglos o arrays
const ar = [1, 2, 3, 4];
console.log("Número: " + ar[2]);
 
// () -> Funciones
function suma(a, b) {
    // Se escribe la función
}

// {} -> Llaves par objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10,
}

if (true) {
    // Todo lo que halla entre llaves
    const constate_2 = "Hola";
}
