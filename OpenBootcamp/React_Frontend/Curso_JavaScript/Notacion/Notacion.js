// Notación

// ; --> delimitar el final de la línea
const b = 4;

b + 4;

// . --> se utiliza en los objetos para acceder a los atributos
// movil.anchura

// [] --> Listas, arreglos o arrays
const ar = [1, 2, 3, 4]
console.log(ar[2]);

// () -->
function suma(a, b) {
    // Se escribe la función
}

// {} --> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
};

if (true) {
    // Todo lo que halla entre llaves
    const constante2 = "Hola";
}
